[pytest]
pythonpath = .
markers =
    gate: sovereign test suite (must be green for release)
    legacy: non-blocking tests (may fail; excluded from gate)
    institution: institutional invariants and constitutional tests
